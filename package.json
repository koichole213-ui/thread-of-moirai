{
  "name": "thread-of-moirai",
  "version": "0.1.1-preview.3",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-isolation=none tests/*.test.js",
    "check": "node scripts/check.mjs"
  }
}
